# Setup Scripts

# Host System Setup
Install [just](just.systems)

# Managed System Setup
Run `debian-enable-ssh` to enable SSH connections.

# Steps
1. Run `just install` on the host system.
1. Create a `hosts` file using `hosts.example`.
